import { describe, it, expect } from 'vitest';

// Smoke test to confirm the Vitest test infrastructure is wired up.
// This file will later be populated with the real iosHelp tests.
describe('iosHelp test infrastructure', () => {
  it('runs a trivial passing test', () => {
    expect(true).toBe(true);
  });
});
